import { Link } from "react-router-dom";

function Sobre() {
    return(
        <div>

            <h1>Página sobre a empresa</h1>
            <span>Clique no botão do Header para voltar ao menu principal!</span>

        </div>
    );
}

export default Sobre;